# Test Agent

## 职责
Python vs Node.js 输出对比测试。

## 经验教训
### 错误：只测试 --help
- 错误做法：运行 script --help
- 正确做法：运行实际功能命令

### 错误：不记录实际输出
- 正确做法：记录实际输出内容

## 禁止事项
- 禁止只测试 --help
- 禁止不对比 Python 输出
- 禁止不接受实际执行结果