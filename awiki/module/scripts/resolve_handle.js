/**
 * resolve_handle.js - resolve-handle.js 的别名
 *
 * 为了与测试导入路径兼容（../../scripts/resolve_handle）
 */

const resolveHandleModule = require('./resolve-handle.js');

module.exports = resolveHandleModule;
